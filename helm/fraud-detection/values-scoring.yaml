image:
  repository: ghcr.io/anthu2708/fraud-detection/scoring
  tag: f2c7100d685810b928085860ff9bd8a92ba4b43b

imagePullSecrets:
  - name: ghcr-secret

env:
  KAFKA_BOOTSTRAP: "kafka:9092"
  MODEL_PATH: "/app/models/isolation_forest_v2.pkl"
  MODEL_VERSION: "v2"
  PYTHONUNBUFFERED: "1"
  METRICS_PORT: "9100"

service:
  enabled: true
  port: 9100

volumeMounts:
  - name: models
    mountPath: /app/models

volumes:
  - name: models
    hostPath:
      path: /fraud-models
